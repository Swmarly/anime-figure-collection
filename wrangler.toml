name = "anime-figure-collection-worker"
main = "_worker.js"
compatibility_date = "2024-06-01"

[assets]
directory = "."

# Uncomment and replace the IDs below after creating your KV namespaces.
# Without this binding the Worker falls back to an in-memory store.
# [[kv_namespaces]]
# binding = "COLLECTION"
# id = "<production-namespace-id>"
# preview_id = "<preview-namespace-id>"
